//Implement an algorithm to find the nth to last element of a singly linked list.

struct Node {
  int data;
  Node* next;
};

int find_nth_to_last(Node* head, int n) {

  Node* current = head;
  while (current != NULL) {
    /* code */
    
  }
}
